{
  "name": "aula",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "test"
  },

  "dependencies": {
    "assert": "^2.1.0",
    "mocha": "^10.2.0",
    "selenium-webdriver": "^4.16.0"
  },

  "scripts": {
    "test": "mocha --timeout 60000"
  }
}
